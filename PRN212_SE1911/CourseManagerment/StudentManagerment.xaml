<Window x:Class="CourseManagerment.StudentManagerment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseManagerment"
        mc:Ignorable="d"
        Title="StudentManagerment" Height="550" Width="800" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <Label Content="Search" Height="30" Width="60"/>
                <TextBox Height="30" Width="300"></TextBox>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Student ID" Margin="5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Name="txtStudentID" Margin="5" IsReadOnly="True"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Rool:" Margin="5"/>
            <TextBox Grid.Row="1" Grid.Column="1" Name="txtRool" Margin="5"/>

            <Label Grid.Row="2" Grid.Column="0" Content="First Name:" Margin="5"/>
            <TextBox Grid.Row="2" Grid.Column="1" Name="txtFirstName" Margin="5"/>

            
            <Label Grid.Row="3" Grid.Column="0" Content="Mid Name:" Margin="5"/>
            <TextBox Grid.Row="3" Grid.Column="1" Name="txtMidName" Margin="5"/>

            
            <Label Grid.Row="4" Grid.Column="0" Content="Last Name:" Margin="5"/>
            <TextBox Grid.Row="4" Grid.Column="1" Name="txtLastName" Margin="5"/>
            <StackPanel Grid.Row="5" Grid.Column="1" Orientation="Horizontal" Margin="5">
                <Button  Content="Add" Width="80" Margin="5" Click="btnAdd"></Button>
                <Button Content="Edit" Width="80" Margin="5" Click="btnEdit"></Button>
                <Button Content="Delete" Width="80" Margin="5" Click="btnDelete"></Button>
                <Button Content="Reset" Width="80" Margin="5" Click="btnReset"></Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="2">
            <DataGrid Name="dvgDisplay" AutoGenerateColumns="False" Margin="5" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Student ID" Binding="{Binding StudentId}" Width="*"/>
                    <DataGridTextColumn Header="Roll" Binding="{Binding Roll}" Width="*"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                    <DataGridTextColumn Header="Mid Name" Binding="{Binding MidName}" Width="*"/>
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </Grid>
</Window>
