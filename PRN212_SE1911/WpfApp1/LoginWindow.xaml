<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="316" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Đăng nhập vào hệ thống" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <Label Content="Tên đăng nhập:"/>
            <TextBox x:Name="UsernameBox" Height="30"/>

            <Label Content="Mật khẩu:" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" Height="30"/>
        </StackPanel>

        <Button Content="Đăng nhập" Grid.Row="2" Height="35" Width="100"
                HorizontalAlignment="Center" Margin="0,20,0,0" Click="Login_Click"/>
    </Grid>
</Window>
