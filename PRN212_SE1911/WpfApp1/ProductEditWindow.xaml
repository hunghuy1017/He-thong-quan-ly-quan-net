<Window x:Class="WpfApp1.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chỉnh sửa sản phẩm" Height="250" Width="400" WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Content="Tên sản phẩm:"/>
            <TextBox x:Name="txtName" Margin="0 0 0 10"/>

            <Label Content="Giá (VNĐ):"/>
            <TextBox x:Name="txtPrice" Margin="0 0 0 10"/>

            <CheckBox x:Name="chkAvailable" Content="Còn bán" Margin="0 0 0 10"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0 10 0 0">
            <Button Content="Lưu" Width="100" Margin="0 0 10 0" Click="Save_Click"/>
            <Button Content="Hủy" Width="100" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
