<Window x:Class="WpfApp1.SessionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sơ đồ máy tính theo loại" Height="650" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#f3f3f3">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,10">
            <TextBlock Text="🖥️ Danh sách máy theo loại" FontSize="22" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="5"/>
            <Button Content="🔄 Làm mới" Width="120" Height="35" 
                    Margin="10,0,0,0" Click="Refresh_Click"
                    Background="#1976D2" Foreground="White"
                    FontWeight="SemiBold" Cursor="Hand"/>
        </DockPanel>

        <!-- Nội dung hiển thị máy -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="TypePanel" Margin="5" />
        </ScrollViewer>
    </Grid>
</Window>
