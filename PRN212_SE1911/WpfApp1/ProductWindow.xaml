<Window x:Class="WpfApp1.ProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        Title="Quản lý sản phẩm" Height="450" Width="700" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <local:IndexConverter x:Key="IndexConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button x:Name="btnAdd" Content="Thêm" Width="100" Margin="0 0 10 0" Click="Add_Click"/>
            <Button Content="Làm mới" Width="100" Click="Reload_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgProducts"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  Margin="0 0 0 10"
                  AlternationCount="1000">
            <DataGrid.Columns>
                <!-- ✅ STT -->
                <DataGridTemplateColumn Header="STT" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow},
                                                      Path=AlternationIndex,
                                                      Converter={StaticResource IndexConverter}}"
                                       HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Giá (VNĐ)" Binding="{Binding Price}" Width="100"/>
                <DataGridCheckBoxColumn Header="Còn bán" Binding="{Binding IsAvailable}" Width="80"/>
                <DataGridTemplateColumn x:Name="ActionColumn" Header="Hành động" Width="160">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Sửa" Width="60" Margin="5 0" Tag="{Binding}" Click="Edit_Click"/>
                                <Button Content="Xóa" Width="60" Margin="5 0" Tag="{Binding}" Click="Delete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" HorizontalAlignment="Right" FontStyle="Italic" Foreground="Gray" Text="Danh sách món ăn/uống"/>
    </Grid>
</Window>
